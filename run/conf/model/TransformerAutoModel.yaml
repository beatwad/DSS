name: TransformerAutoModel
params:
  model_name: ${dir.t_automodel_dir} #  Since the internet is off during inference, 
                                 #  use the config obtained from dataset
  hidden_size: 120
  stride: ${downsample_rate}
