# activate
. .venv/bin/activate

# preprocess data
rye run python run/prepare_data.py -m phase=train,test

# train model
rye run python run/train.py -m downsample_rate=2 duration=5760 exp_name=exp001 batch_size=32

# grid search
rye run python run/train.py -m duration=100,1000,2000 sigma=4,5,6,8,10

# upload model
rye run python tools/upload_dataset.py


